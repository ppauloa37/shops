var adadshop = {
  preco: null,
  produtos: {
    produto: [{
      nome:"CABO AZUL",
      custo:"2,48R$",
    }]
  },
    precopt(custo) {
      if (custo) {
        preco = custo
      } else {
        console.error("Parece que custo is not defined")
      }
    },
    frete(desconto,produtonum) {
      if (desconto && produto) {
        produtos.produto[produtonum].custo - desconto
      }
    }
  }

  
    var enviar = document.getElementById("enviar")
    var lista = document.getElementById("lista")
    var image = document.getElementById("image").value;
